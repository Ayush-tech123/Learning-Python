#numbers

#python we dont need to specify type
temp = 5
print(temp)

#but we can specify if we wish using pre defined functions
converted_to_string = str(temp)
print(temp)

#Absolute function = converting any number to its positive equivalent
temp = -5
print(temp)
print(abs(temp))

#Power function
temp = pow(3,2)
print(temp)

#max function
temp = max(3,-9)
print(temp)

#min function
temp = min(3,-9)
print(temp)

#rounding function
temp = 3.9
print(round(temp))